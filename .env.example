# Frontend data mode
# local keeps using browser local storage
# http calls the backend API shell
VITE_GLIDE_API_MODE=local
VITE_GLIDE_API_BASE_URL=http://localhost:8787

# Backend API shell
PORT=8787
HOST=0.0.0.0
