# Supabase mode only.
# Keep real VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in .env.local.
# Do not put placeholder Supabase credentials here, because mode-specific env files can override .env.local.
VITE_GLIDE_API_MODE=supabase
